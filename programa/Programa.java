package programa;

import fachada.Fachada;
import treinador.*;
import jogador.*;
import time.*;
import habilidade.*;

public class Programa {
    public static void main(String[] args) {
        //Aqui vamos cadastrar tudo que o sistema vai utilizar
        //Também iremos fazer todas as relações entre as classes

        //Ex: Comprar jogador, vamos usar o remover jogador, e cadastrar jogador

        //Obs 1: O teste "main" de treinador está pronto, e se encontra na pasta "teste"
        //Obs 2: Façam os testes das classes de vocês e coloquem na pasta teste, depois irimos juntar tudo aqui
        //Obs 3: Não deixem para tentar resolver tudo de ultima hora, qualquer duvida pergunta no grupo do whatsapp.
    }
}
